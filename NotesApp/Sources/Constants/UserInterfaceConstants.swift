let NOTE = Note(
    id: NOTE_MODEL.id, title: NOTE_MODEL.title, body: NOTE_MODEL.body,
    createdAt: NOTE_MODEL.createdAt, updatedAt: NOTE_MODEL.updatedAt,
    userId: NOTE_MODEL.userId)

let USER = User(id: USER_MODEL.id, username: USER_MODEL.username)

let NOT_VALID_USERNAME_ERROR_MESSAGE = "Not valid username"

let NO_AVAILABLE_INTERNET_ERROR_MESSAGE = "Not internet connection"

let NO_NOTES_LABEL = "No notes"

let NOTE_BODY_TEXT_INPUT_PLACEHOLDER = "Body"
